version: "3.9"

services:
  backend:
    build: ./backend
    container_name: dockerdev_backend
    ports:
      - "8000:8000"
    env_file:
      - ./backend/.env
    environment:
      DOCKERDEV_WORK_DIR: /work
    extra_hosts:
      - "host.docker.internal:host-gateway"
    volumes:
      - ./tmp:/work
      # NOTE: On Windows, this requires Docker Desktop with WSL 2 backend.
      # May not work on systems without Docker Desktop support.
      - /var/run/docker.sock:/var/run/docker.sock
    networks:
      - dockerdev-net
    restart: unless-stopped

  frontend:
    build: ./frontend
    container_name: dockerdev_frontend
    ports:
      - "3000:80"
    depends_on:
      - backend
    networks:
      - dockerdev-net
    restart: unless-stopped

networks:
  dockerdev-net:
    driver: bridge
